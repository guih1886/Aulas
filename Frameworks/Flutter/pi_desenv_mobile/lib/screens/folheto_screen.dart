import 'package:flutter/material.dart';

class FolhetoScreen extends StatelessWidget {
  const FolhetoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
        child: Container(
      color: Colors.blue,
    ));
  }
}
